using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Bitly.Tests
{
    public class Program
    {
        //for some reason if we creating .net core dll project we can not referenced .net core mvc project, so it's console ((
        public static void Main(string[] args)
        {
        }
    }
}
